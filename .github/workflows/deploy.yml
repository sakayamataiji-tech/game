name: Deploy to GitHub Pages

on:
  push:
    branches: [main, claude/nlh-dealer-trainer-h5rzpe]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            nlh-dealer-trainer/package-lock.json
      - run: npm ci
      - run: npm test
      - run: npm run build
      - name: Build NLH Dealer Trainer (static export)
        working-directory: nlh-dealer-trainer
        env:
          NEXT_PUBLIC_BASE_PATH: /game/nlh-dealer-trainer
        run: |
          npm ci
          npm test
          npm run build
      - name: Bundle trainer under /game/nlh-dealer-trainer/
        run: |
          mkdir -p dist/nlh-dealer-trainer
          cp -r nlh-dealer-trainer/out/. dist/nlh-dealer-trainer/
      - uses: actions/configure-pages@v5
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
